grumphp:
    ascii:
    tasks:
        git_blacklist:
            keywords:
                - '(var_|)dump\(.*\);*'
                - 'die(;|\(.*\));*'
            regexp_type: P
        phpcs:
        phpstan:
            level: 1
